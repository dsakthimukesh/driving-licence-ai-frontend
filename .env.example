# Base URL for backend API requests.
# Leave empty or set to relative path in local development to use Vite server proxy (eliminates CORS errors).
# Set to full production backend URL (e.g., https://api.yourdomain.com) for production builds.
VITE_API_BASE_URL=
